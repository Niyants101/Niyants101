{
  "name": "niyant-random-profile",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "connect-profile": "node scripts/set-profile-worker-url.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
